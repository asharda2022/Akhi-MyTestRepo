name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
          echo "Hello World"
          echo "Hello World piped"
      - name: say GoodBye
        run: echo "Done - byeeeee!"
